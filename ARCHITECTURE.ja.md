# アーキテクチャと設計

このドキュメントは、Live2D エフェクト拡張機能の背後にある技術的なアーキテクチャと設計決定を説明します。

## 概要

Live2D エフェクト拡張機能は、Canvas 2D APIとバイリニア補間を使用したメッシュベースの画像変形を実装しています。画像変形を制御するためのScratchライクなブロックインターフェースを提供します。

## アーキテクチャ図

```
┌─────────────────────────────────────────────────────────────┐
│                    TurboWarp/Scratch VM                     │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                  Live2D エフェクト拡張機能                  │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │                   ブロックインターフェース             │ │
│  │  • 画像読み込み                                        │ │
│  │  • 頂点制御                                            │ │
│  │  • プリセット変形                                      │ │
│  │  • 描画コマンド                                        │ │
│  └───────────────┬───────────────────────────────────────┘ │
│                  │                                          │
│                  ▼                                          │
│  ┌───────────────────────────────────────────────────────┐ │
│  │              変形エンジン                             │ │
│  │                                                        │ │
│  │  ┌──────────────┐    ┌──────────────┐               │ │
│  │  │  頂点        │    │   プリセット │               │ │
│  │  │  管理        │◄───┤   生成器     │               │ │
│  │  └──────┬───────┘    └──────────────┘               │ │
│  │         │                                             │ │
│  │         ▼                                             │ │
│  │  ┌──────────────────────────────────┐               │ │
│  │  │   メッシュ分割エンジン           │               │ │
│  │  │   • 10x10グリッド分割            │               │ │
│  │  │   • バイリニア補間               │               │ │
│  │  └──────┬───────────────────────────┘               │ │
│  │         │                                             │ │
│  │         ▼                                             │ │
│  │  ┌──────────────────────────────────┐               │ │
│  │  │   Canvas変形                     │               │ │
│  │  │   • アフィン変換                 │               │ │
│  │  │   • セルごとのレンダリング       │               │ │
│  │  └──────┬───────────────────────────┘               │ │
│  └─────────┼──────────────────────────────────────────┘ │
│            │                                              │
└────────────┼──────────────────────────────────────────────┘
             │
             ▼
    ┌────────────────────┐
    │   Canvas 2D API    │
    │   • 画像描画       │
    │   • 変形           │
    └────────────────────┘
```

## コンポーネントの説明

### 1. ブロックインターフェースレイヤー

**責務**: Scratchライクなブロックインターフェースを提供

**コンポーネント**:
- `getInfo()`: ブロック構造とメタデータを定義
- ブロックハンドラ：ブロックが使用されたときに実行されるメソッド
- メニュー定義：ブロックのドロップダウンオプション

**主要メソッド**:
- `loadImageFromCostume()`: Scratchスプライトから画像を読み込む
- `loadImageFromURL()`: WebのURLから画像を読み込む
- `setVertexPosition()`: 個別の頂点座標を設定
- `applyPreset()`: プリセット変形を適用
- `drawTransformed()`: 変形した画像をレンダリング

### 2. 変形エンジン

**責務**: 頂点位置と変形ロジックを管理

**コンポーネント**:

#### 頂点管理
- 4つの頂点位置を保存（左上、右上、左下、右下）
- 内部的に正規化された0-1座標系を使用
- 創造的な効果のために任意の座標値をサポート

#### プリセット生成器
- 4つのプリセット変形タイプを実装：
  - **台形**: 上辺を狭める
  - **遠近法**: 3D奥行き効果を作成
  - **波**: 正弦波の歪みを適用
  - **膨張**: 中心から外側に拡張

#### メッシュ分割エンジン
- 画像を10×10グリッドに分割（100セル）
- 各グリッド交点にバイリニア補間を適用
- 中間頂点位置を計算

**アルゴリズム**:
```javascript
// バイリニア補間の公式
x = tl.x * (1-u) * (1-v) + 
    tr.x * u * (1-v) + 
    bl.x * (1-u) * v + 
    br.x * u * v

y = tl.y * (1-u) * (1-v) + 
    tr.y * u * (1-v) + 
    bl.y * (1-u) * v + 
    br.y * u * v

ここで:
  u, v = グリッド内の正規化された位置（0-1）
  tl, tr, bl, br = 左上、右上、左下、右下の頂点
```

#### Canvas変形
- 各メッシュセルをアフィン変換に変換
- Canvas 2Dコンテキストに変換行列を適用
- 変形でソース画像セグメントをレンダリング

## データフロー

### 画像読み込みフロー

```
ユーザーブロック → loadImageFromCostume/URL
                    ↓
          Imageオブジェクトを作成
                    ↓
           画像データを読み込む
                    ↓
        this.imageDataに保存
                    ↓
         Canvasを初期化
```

### 変形フロー

```
ユーザーが頂点/プリセットを設定
          ↓
    this.verticesを更新
          ↓
    ユーザーが描画を呼び出す
          ↓
   10x10グリッドをループ
          ↓
   各セルについて:
     1. ソース矩形を計算
     2. 目的地の四角形を補間
     3. アフィン変換を計算
     4. 画像セグメントを描画
          ↓
    変形完了した画像
```

### プリセット適用フロー

```
ユーザーがプリセット + 強さを選択
          ↓
    元の頂点にリセット
          ↓
    プリセットアルゴリズムを適用
    (強さパラメータに基づく)
          ↓
    this.verticesを更新
          ↓
    描画の準備完了
```

## 座標系

### 内部座標系（正規化）

拡張機能は内部的に頂点位置に正規化された0-1座標系を使用：

```
(0,0) ──────────► (1,0)
  │                 │
  │                 │
  │     画像        │
  │                 │
  │                 │
  ▼                 ▼
(0,1) ──────────► (1,1)
```

**メリット**:
- 解像度非依存
- 任意の画像サイズに簡単にスケーリング
- 補間計算の簡素化

### ユーザー座標系

ユーザーは任意の数値を使用して頂点位置を設定可能：
- 正/負の数
- 0-1の範囲外の値
- 小数値

これらはレンダリング時にピクセル座標に変換されます：
```javascript
pixelX = vertexX * imageWidth
pixelY = vertexY * imageHeight
```

## 変形の数学

### バイリニア補間

四角形内の任意の点(u, v)について、位置は4つの角の加重平均を使用して計算されます：

```
P(u,v) = P₀(1-u)(1-v) + P₁u(1-v) + P₂(1-u)v + P₃uv

ここで:
  P₀ = 左上頂点
  P₁ = 右上頂点
  P₂ = 左下頂点
  P₃ = 右下頂点
  u = 水平パラメータ（0-1）
  v = 垂直パラメータ（0-1）
```

### アフィン変換行列

各メッシュセルについて、アフィン変換を計算：

```
│ a  c  e │   │ x │   │ x' │
│ b  d  f │ × │ y │ = │ y' │
│ 0  0  1 │   │ 1 │   │ 1  │

ここで:
  a, b = 水平基底ベクトル
  c, d = 垂直基底ベクトル
  e, f = 平行移動ベクトル
```

コードでは：
```javascript
ctx.transform(
  dx1/sw,  // a: 水平スケール/スキュー
  dy1/sw,  // b: 水平基底の垂直成分
  dx2/sh,  // c: 垂直基底の水平成分
  dy2/sh,  // d: 垂直スケール/スキュー
  tl.x,    // e: x平行移動
  tl.y     // f: y平行移動
);
```

## パフォーマンスに関する考慮事項

### 最適化戦略

1. **メッシュ分割**: 10×10グリッドが品質とパフォーマンスのバランスを取る
   - 少ない分割 = 速いが粗い
   - 多い分割 = 遅いが滑らか

2. **Canvasの再利用**: すべての変形に単一のCanvasを再利用
   - Canvas オブジェクトの作成/破棄を避ける
   - メモリ割り当てを減らす

3. **遅延評価**: 変形は描画時にのみ計算
   - 頂点の設定は即座
   - 描画呼び出しのみが計算をトリガー

### パフォーマンス特性

| 画像サイズ | メッシュグリッド | 変形時間 | 推奨用途 |
|-----------|-----------------|---------|----------|
| 100x100   | 10x10          | ~5ms    | リアルタイムアニメーション |
| 200x200   | 10x10          | ~10ms   | リアルタイムアニメーション |
| 500x500   | 10x10          | ~25ms   | インタラクティブ使用 |
| 1000x1000 | 10x10          | ~50ms   | 静的/時々 |

*時間は概算でブラウザ/ハードウェアによって異なります*

## メモリ管理

### メモリ使用量

- **Canvas**: 幅 × 高さ × 4バイト（RGBA）
- **画像データ**: 元の画像サイズ
- **頂点データ**: 無視できる程度（4点 × 2座標）

### メモリ最適化

1. すべての変形で共有される単一のCanvasインスタンス
2. 画像データは参照として保存（コピーされない）
3. 中間バッファは保持されない

## エラー処理

### エラーシナリオ

1. **画像読み込み失敗**
   - コスチュームが見つからない
   - URLがアクセス不可またはCORSブロック
   - 無効な画像形式
   - **処理**: エラーをログし、優雅に戻る

2. **無効な頂点位置**
   - 範囲外の値
   - NaNまたはundefinedの値
   - **処理**: クランプまたは無視、クラッシュしない

3. **Canvas操作**
   - 変換行列の特異点
   - 範囲外への描画
   - **処理**: try-catchブロック、サイレント失敗

## 拡張ポイント

### 新しいプリセットの追加

新しい変形プリセットを追加するには：

1. `PRESET_TYPES`定数にプリセット名を追加
2. `getInfo().menus.presetMenu`にメニュー項目を追加
3. `applyPreset()`のswitch文にケースを実装
4. READMEとEXAMPLESでドキュメント化

例：
```javascript
case 'mypreset':
  const factor = strength * 0.5;
  this.vertices.topleft.x = factor;
  // ... 他の頂点を計算
  break;
```

### 新しいブロックの追加

新しいブロックタイプを追加するには：

1. `getInfo().blocks`配列でブロックを定義
2. クラスでハンドラメソッドを実装
3. 必要に応じて`getInfo().menus`でメニューを追加
4. ドキュメントを更新

### メッシュ解像度の変更

メッシュ品質を変更するには：

```javascript
// _drawMeshTransformメソッド内
const divisions = 10; // この値を変更

// 高い = より良い品質、遅い
// 低い = より悪い品質、速い
```

## 設計決定

### なぜWebGLではなくCanvas 2Dか？

**理由**:
1. **シンプルさ**: Canvas 2Dの方が実装とデバッグが簡単
2. **互換性**: より良いブラウザサポート
3. **十分なパフォーマンス**: 典型的な使用例には適切
4. **より簡単なメンテナンス**: より多くの開発者がCanvas 2Dを理解

**将来**: パフォーマンスオプションとしてWebGLレンダラーを追加可能

### なぜ10×10メッシュグリッドか？

**分析**:
- 5×5: 粗すぎ、可視アーティファクト
- 10×10: 良いバランス（選択）
- 20×20: 視覚的改善が最小、4倍遅い
- 50×50: ほとんどの場合オーバーキル、25倍遅い

### なぜ正規化座標か？

**メリット**:
1. 解像度非依存
2. スケーリングが容易
3. 数学の簡素化
4. 画像間で一貫した動作

**トレードオフ**: 追加の変換ステップが必要だが、柔軟性のために価値あり

## テスト戦略

### 手動テスト

1. **画像読み込み**: 様々な形式とソースをテスト
2. **変形**: 各プリセットを様々な強さで検証
3. **頂点制御**: 極端な値とエッジケースをテスト
4. **描画**: 様々な位置とサイズでテスト
5. **パフォーマンス**: 異なる画像サイズでテスト

### ブラウザテスト

複数のブラウザでテスト：
- Chrome/Chromium
- Firefox
- Safari
- TurboWarp Desktop

### エッジケース

- 空/欠落したコスチューム
- 非常に大きい画像（1000×1000以上）
- 非常に小さい画像（10×10）
- 極端な頂点位置（負、非常に大きい）
- 急速な連続変形
- ゼロサイズの寸法

## 将来の拡張

### 潜在的な追加機能

1. **3D変形**: 真の透視投影
2. **より多くのプリセット**: 回転、スパイラル、ピンチ、渦巻き
3. **アニメーションヘルパー**: トゥイーン関数、イージング
4. **メッシュエクスポート**: 頂点設定の保存/読み込み
5. **WebGLレンダラー**: より良いパフォーマンスのため
6. **テクスチャマッピング**: メッシュにテクスチャを適用
7. **マルチポイント制御**: 4つ以上の頂点
8. **ベジェ曲線**: 曲線エッジの変形

### API拡張

将来可能なブロック：
- `頂点設定を保存`
- `頂点設定を読み込む`
- `[n]秒かけてプリセットにアニメート`
- `変換行列を取得`
- `カスタム行列を適用`

## 参考文献

### 技術概念

- **バイリニア補間**: [Wikipedia](https://en.wikipedia.org/wiki/Bilinear_interpolation)
- **アフィン変換**: [Wikipedia](https://en.wikipedia.org/wiki/Affine_transformation)
- **Canvas 2D API**: [MDN](https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D)

### インスピレーション

- **Live2D**: 2Dアニメーション用のポリゴン変形
- **Scratch**: ブロックベースのプログラミングインターフェース
- **TurboWarp**: 拡張されたScratchランタイム

## まとめ

Live2D エフェクト拡張機能は、TurboWarpでの画像変形のための強力でありながらシンプルなインターフェースを提供します。メッシュベースの変形とバイリニア補間を使用することで、良好なパフォーマンスを維持しながら滑らかで高品質の結果を達成します。

アーキテクチャは以下のように設計されています：
- **拡張可能**: 新機能を追加しやすい
- **保守可能**: 関心事の明確な分離
- **パフォーマンス**: インタラクティブ使用のために最適化
- **ユーザーフレンドリー**: 直感的なブロックインターフェース

アーキテクチャに関する質問や提案があれば、GitHubで問題を開いてください。
