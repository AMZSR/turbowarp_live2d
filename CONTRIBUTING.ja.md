# Live2D エフェクト拡張機能への貢献

Live2D エフェクト拡張機能for TurboWarpへの貢献に興味を持っていただきありがとうございます！このドキュメントは、貢献者のためのガイドラインと情報を提供します。

## 目次

1. [行動規範](#行動規範)
2. [貢献方法](#貢献方法)
3. [開発環境のセットアップ](#開発環境のセットアップ)
4. [コーディング規約](#コーディング規約)
5. [テスト](#テスト)
6. [プルリクエストのプロセス](#プルリクエストのプロセス)
7. [機能リクエスト](#機能リクエスト)
8. [バグ報告](#バグ報告)

## 行動規範

### 私たちの誓い

私たちは、経験レベル、背景、アイデンティティに関わらず、すべての貢献者にとって歓迎的で包括的な環境を提供することに尽力します。

### 期待される行動

- コミュニケーションにおいて敬意を持ち建設的である
- 新規参加者を歓迎し、始めるのを手伝う
- 建設的な批判を優雅に受け入れる
- コミュニティとプロジェクトにとって最善のことに焦点を当てる
- 他のコミュニティメンバーに共感を示す

### 受け入れられない行動

- ハラスメント、差別、または攻撃的なコメント
- 荒らしまたは侮辱的/蔑視的なコメント
- 公的または私的なハラスメント
- 許可なく他者の個人情報を公開する
- 不適切と合理的に考えられるその他の行為

## 貢献方法

### 貢献の種類

様々な種類の貢献を歓迎します：

1. **バグ修正**: 既存のコードの問題を修正
2. **新機能**: 新しい変形エフェクトやブロックを追加
3. **ドキュメント**: ドキュメントを改善または翻訳
4. **例**: 新しい使用例を追加
5. **パフォーマンス**: 既存のコードを最適化
6. **テスト**: テストを追加または改善

### 始め方

1. リポジトリをフォーク
2. ローカルにフォークをクローン
3. 貢献のために新しいブランチを作成
4. 変更を加える
5. 変更を十分にテスト
6. プルリクエストを提出

## 開発環境のセットアップ

### 前提条件

- モダンなWebブラウザ（Chrome、Firefox、Safari、またはEdge）
- JavaScript（ES6+）の基本知識
- Canvas APIの知識（役立つが必須ではない）
- TurboWarp拡張機能システムの理解

### ローカル開発

1. リポジトリをクローン：
   ```bash
   git clone https://github.com/AMZSR/turbowarp_live2d.git
   cd turbowarp_live2d
   ```

2. シンプルなHTTPサーバーを使用してローカルで拡張機能を提供：
   ```bash
   # Python 3を使用
   python -m http.server 8000
   
   # Node.jsを使用
   npx http-server -p 8000
   ```

3. TurboWarpで拡張機能を読み込む：
   - 拡張機能に移動
   - 「カスタム拡張機能」を選択
   - 入力：`http://localhost:8000/extensions/live2d-effect.js`

4. TurboWarpで変更をテスト

### ファイル構造

```
turbowarp_live2d/
├── extensions/
│   └── live2d-effect.js    # メイン拡張機能ファイル
├── CONTRIBUTING.ja.md       # このファイル
├── EXAMPLES.ja.md          # 使用例（日本語）
├── LICENSE                 # MITライセンス
├── README.md               # メインドキュメント（英語）
├── README.ja.md            # 日本語ドキュメント
└── test.html               # ビジュアルテストページ
```

## コーディング規約

### JavaScriptスタイルガイド

- 適切な場所でES6+機能を使用
- 一貫したインデント（スペース2つ）に従う
- 意味のある変数名と関数名を使用
- 複雑なロジックにコメントを追加
- 関数を焦点を絞って簡潔に保つ

### コード構成

```javascript
// 良い例：単一責任を持つ明確な関数
function calculateVertexPosition(u, v, vertices) {
  return {
    x: interpolateX(u, v, vertices),
    y: interpolateY(u, v, vertices)
  };
}

// 悪い例：混在した関心事を持つ不明瞭な関数
function doStuff(a, b, c) {
  // 複数の無関係な操作
}
```

### ドキュメント

- 公開メソッドにJSDocコメントを追加
- パラメータと戻り値を文書化
- 複雑な機能の使用例を含める
- 新機能を追加する際はREADME.mdを更新

例：
```javascript
/**
 * 画像にプリセット変形を適用
 * @param {Object} args - ブロック引数
 * @param {string} args.PRESET - プリセット名（trapezoid、perspective、wave、bulge）
 * @param {number} args.STRENGTH - 変形の強さ（0-100）
 */
applyPreset(args) {
  // 実装
}
```

### 命名規則

- **クラス**: PascalCase（例：`Live2DEffect`）
- **関数**: camelCase（例：`loadImageFromURL`）
- **定数**: UPPER_SNAKE_CASE（例：`VERTEX_NAMES`）
- **プライベートメソッド**: アンダースコアで接頭辞（例：`_initCanvas`）

## テスト

### 手動テストチェックリスト

プルリクエストを提出する前に、以下をテスト：

- [ ] TurboWarpで拡張機能がエラーなく読み込まれる
- [ ] すべてのブロックがブロックパレットに表示される
- [ ] 画像の読み込みが機能する（コスチュームとURLの両方）
- [ ] 頂点位置の設定が正しく機能する
- [ ] すべてのプリセット変形が期待通りに機能する
- [ ] 描画機能が正しくレンダリングする
- [ ] リセット機能が機能する
- [ ] 通常操作中にコンソールエラーがない
- [ ] 様々な画像サイズでパフォーマンスが許容範囲

### 異なるブラウザでのテスト

複数のブラウザで変更をテスト：
- Chrome/Chromium
- Firefox
- Safari（利用可能な場合）
- TurboWarp Desktop

### ビジュアルテスト

含まれている`test.html`ファイルを使用して変形を視覚的に確認：

```bash
# テストページを提供
python -m http.server 8000

# ブラウザで開く
open http://localhost:8000/test.html
```

## プルリクエストのプロセス

### 提出前

1. **十分にテスト**: すべての機能が動作することを確認
2. **ドキュメントを更新**: 必要に応じてドキュメントを追加または更新
3. **コードスタイルを確認**: コーディング規約に従う
4. **例を追加**: 新機能の使用例を含める
5. **明確なコミットメッセージを書く**: 何をなぜ行ったかを説明

### コミットメッセージの形式

```
<type>: <subject>

<body>

<footer>
```

タイプ：
- `feat`: 新機能
- `fix`: バグ修正
- `docs`: ドキュメントの変更
- `style`: コードスタイルの変更（フォーマットなど）
- `refactor`: コードのリファクタリング
- `perf`: パフォーマンスの改善
- `test`: テストの追加または更新
- `chore`: メンテナンスタスク

例：
```
feat: 回転プリセット変形を追加

- 回転変形プリセットを実装
- 回転強度パラメータを追加
- 回転例でドキュメントを更新
- 回転効果のビジュアルテストを追加

Closes #123
```

### プルリクエストテンプレート

プルリクエストを提出する際は、以下を含める：

1. **説明**: このPRは何をするか？
2. **動機**: なぜこの変更が必要か？
3. **変更**: 行った具体的な変更のリスト
4. **テスト**: どのようにテストしたか？
5. **スクリーンショット**: ビジュアルの変更には、前後を含める
6. **破壊的変更**: 破壊的変更があれば注記
7. **関連する問題**: 関連する問題を参照

### レビュープロセス

1. プルリクエストを提出
2. メンテナーのレビューを待つ
3. フィードバックに対応
4. 承認されたら、PRはマージされます

## 機能リクエスト

### 新機能の提案

1. 既存の問題を確認して重複を避ける
2. 「機能リクエスト」ラベルで新しい問題を開く
3. 機能を明確に説明
4. 使用例とメリットを説明
5. 可能であれば例を提供

### 機能リクエストテンプレート

```markdown
## 機能の説明
[機能の明確な説明]

## 使用例
[なぜこの機能が必要か？]

## 提案された解決策
[この機能はどのように機能するか？]

## 例
[使用例またはモックアップ]

## 検討した代替案
[検討した他のアプローチ]
```

## バグ報告

### 報告前

1. 既存の問題を検索
2. バグを再現してみる
3. 可能であれば複数のブラウザでテスト
4. 関連情報を収集

### バグ報告テンプレート

```markdown
## バグの説明
[バグの明確な説明]

## 再現手順
1. [最初のステップ]
2. [2番目のステップ]
3. [...]

## 期待される動作
[何が起こるべきか]

## 実際の動作
[実際に何が起こるか]

## 環境
- ブラウザ：[Chrome 120、Firefox 115など]
- TurboWarpバージョン：[Desktop/Online]
- 拡張機能バージョン：[1.0.0]

## スクリーンショット/コンソールエラー
[該当する場合]

## 追加のコンテキスト
[その他の関連情報]
```

## 新しい変形プリセットの追加

新しい変形プリセットを追加するには：

1. `PRESET_TYPES`定数にプリセット名を追加
2. `getInfo().menus.presetMenu`にメニュー項目を追加
3. `applyPreset()`で変形ロジックを実装
4. ドキュメントと例を追加
5. 十分にテスト

例：
```javascript
case 'rotation':
  // 回転変形を計算
  const angle = strength * Math.PI / 4; // 最大45度
  const cos = Math.cos(angle);
  const sin = Math.sin(angle);
  
  // 中心を軸に回転を適用
  this.vertices.topleft.x = 0.5 - cos * 0.5 + sin * 0.5;
  this.vertices.topleft.y = 0.5 - sin * 0.5 - cos * 0.5;
  // ... (他の頂点)
  break;
```

## 新しいブロックの追加

新しいブロックを追加するには：

1. `getInfo().blocks`にブロック定義を追加
2. 対応するメソッドを実装
3. 必要なメニューを追加
4. ドキュメントを更新
5. 使用例を追加

例：
```javascript
{
  opcode: 'getTransformStrength',
  blockType: Scratch.BlockType.REPORTER,
  text: '現在の変形の強さ',
  // クラスメソッドでの実装
}
```

## パフォーマンス最適化ガイドライン

- 最適化前にコードをプロファイル
- 頻繁に呼び出される関数に焦点を当てる
- メッシュ分割のトレードオフを考慮
- 可能な場合は計算をキャッシュ
- 不必要なCanvas操作を避ける
- 様々な画像サイズでテスト

## ドキュメントガイドライン

### ドキュメントの作成

- 明確でシンプルな言語を使用
- コード例を含める
- 役立つ場合は視覚的な例を追加
- 可能であれば日本語に翻訳
- ドキュメントを最新の状態に保つ

### ドキュメントセクション

1. **README.md**: メインドキュメント
2. **README.ja.md**: 日本語翻訳
3. **EXAMPLES.ja.md**: 詳細な使用例
4. **CONTRIBUTING.ja.md**: このファイル
5. **コードコメント**: インラインドキュメント

## 質問がありますか？

貢献に関して質問がある場合：

1. 既存のドキュメントを確認
2. クローズされた問題を検索
3. 質問で新しい問題を開く
4. 「質問」ラベルでタグ付け

## 認識

貢献者は以下で認識されます：
- Gitコミット履歴
- プルリクエストのディスカッション
- リリースノート（重要な貢献の場合）

Live2D エフェクト拡張機能への貢献ありがとうございます！
