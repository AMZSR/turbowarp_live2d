# クイックスタートガイド - Live2D エフェクト拡張機能

数分でLive2D エフェクト拡張機能を使い始めましょう！

## 🚀 インストール

### ステップ1: TurboWarpを開く
[https://turbowarp.org/](https://turbowarp.org/) にアクセスします

### ステップ2: 拡張機能を読み込む
1. 左下の**拡張機能**ボタン（フォルダアイコン）をクリック
2. **カスタム拡張機能**を選択
3. 拡張機能ファイルのURLを入力：
   ```
   https://raw.githubusercontent.com/AMZSR/turbowarp_live2d/main/extensions/live2d-effect.js
   ```
4. **読み込む**をクリック

### ステップ3: ブロックを探す
ブロックパレットに新しい**Live2D Effect**カテゴリが表示されます。ピンク/赤の配色になっています。

## 📝 最初の変形

### 例1: シンプルな台形エフェクト

基本的な台形変形を作成しましょう：

```scratch
緑の旗がクリックされたとき
コスチューム [costume1 ▾] から画像を読み込む
[台形 ▾] 変形を適用 強さ: (50)%
変形した画像を x: (0) y: (0) に描画
```

**動作内容：**
1. スプライトのコスチュームから画像を読み込みます
2. 50%の強さで台形エフェクトを適用します
3. ステージの中央に変形した画像を描画します

### 例2: アニメーション波

旗がはためくエフェクトを作成：

```scratch
緑の旗がクリックされたとき
コスチューム [costume1 ▾] から画像を読み込む
ずっと
  [波 ▾] 変形を適用 強さ: ((50) + ((タイマー の sin) × (30)))
  変形した画像を x: (0) y: (0) に描画
end
```

**動作内容：**
- 継続的な波打つモーションを作成
- 滑らかなアニメーションのためにサイン波を使用
- 強さは20%から80%の間で変化

### 例3: インタラクティブな遠近法

マウスで変形を制御：

```scratch
緑の旗がクリックされたとき
コスチューム [costume1 ▾] から画像を読み込む
ずっと
  [強さ v] を (((マウスのx座標) + (240)) / (4.8)) にする
  [遠近法 ▾] 変形を適用 強さ: (強さ)%
  変形した画像を x: (0) y: (0) に描画
end
```

**動作内容：**
- マウスの位置で遠近法エフェクトを制御
- 左側：エフェクトなし、右側：最大エフェクト
- リアルタイムのインタラクティブな変形

## 🎨 利用可能な変形

### 台形
画像の上辺を狭めます。
- **最適な用途：** 奥行き感、屋根の形状、遠くへ続く道路
- **強さの範囲：** 0-100%

### 遠近法
3D遠近感のエフェクトを作成。
- **最適な用途：** 壁、建物、背景の風景
- **強さの範囲：** 0-100%

### 波
正弦波による歪み効果を適用。
- **最適な用途：** 旗、水面の反射、弾性のあるオブジェクト
- **強さの範囲：** 0-100%

### 膨張
中心から外側に膨らむ効果。
- **最適な用途：** 風船、泡、拡大効果
- **強さの範囲：** 0-100%

## 🎯 一般的な使用例

### 使用例1: キャラクターの表情

キャラクターの頭を傾ける：

```scratch
緑の旗がクリックされたとき
コスチューム [顔 ▾] から画像を読み込む
頂点 [左上 ▾] を x: (-10) y: (5) に設定
頂点 [右上 ▾] を x: (110) y: (-5) に設定
頂点 [左下 ▾] を x: (0) y: (100) に設定
頂点 [右下 ▾] を x: (100) y: (100) に設定
変形した画像を x: (0) y: (0) に描画
```

### 使用例2: 画面の揺れ

衝撃エフェクトを作成：

```scratch
[スペース v] キーが押されたとき
コスチューム [画面 ▾] から画像を読み込む
(10) 回繰り返す
  頂点 [左上 ▾] を x: ((-5) から (5) までの乱数) y: ((-5) から (5) までの乱数) に設定
  頂点 [右上 ▾] を x: ((100) + ((-5) から (5) までの乱数)) y: ((-5) から (5) までの乱数) に設定
  頂点 [左下 ▾] を x: ((-5) から (5) までの乱数) y: ((100) + ((-5) から (5) までの乱数)) に設定
  頂点 [右下 ▾] を x: ((100) + ((-5) から (5) までの乱数)) y: ((100) + ((-5) から (5) までの乱数)) に設定
  変形した画像を x: (0) y: (0) に描画
  (0.03) 秒待つ
end
すべての変形をリセット
変形した画像を x: (0) y: (0) に描画
```

### 使用例3: スムーズな遷移

通常から変形へのアニメーション：

```scratch
緑の旗がクリックされたとき
コスチューム [スプライト ▾] から画像を読み込む
(30) 回繰り返す
  [進行度 v] を ((i) / (30)) にする
  [強さ v] を ((進行度) × (100)) にする
  [膨張 ▾] 変形を適用 強さ: (強さ)%
  変形した画像を x: (0) y: (0) に描画
  (0.03) 秒待つ
end
```

## 💡 ヒント＆トリック

### ヒント1: シンプルに始める
手動頂点制御を試す前に、プリセット変形から始めましょう。

### ヒント2: 変数を使用する
複雑なアニメーションのために変数に頂点位置を保存：
```scratch
[左上X v] を (0) にする
[左上Y v] を (0) にする
頂点 [左上 ▾] を x: (左上X) y: (左上Y) に設定
```

### ヒント3: こまめにリセット
エフェクトを切り替える時は`すべての変形をリセット`を使用して最初から始めましょう。

### ヒント4: 強さを調整
画像によって最適な強さレベルが異なります。実験してみましょう！
- 控えめなエフェクト：20-40%
- 中程度のエフェクト：40-70%
- 強いエフェクト：70-100%

### ヒント5: Scratchエフェクトと組み合わせる
Live2D変形と組み込みのScratchエフェクトを重ねる：
```scratch
[幽霊 ▾] の効果を (50) にする
[波 ▾] 変形を適用 強さ: (60)%
変形した画像を x: (0) y: (0) に描画
```

## 🐛 トラブルシューティング

### 問題: 拡張機能が読み込まれない
**解決方法：** 通常のScratchではなくTurboWarpを使用していることを確認し、正しいURLを入力してください。

### 問題: 画像が表示されない
**解決方法：** 
1. 最初に画像を読み込んだことを確認
2. コスチューム名のスペルが正しいか確認（大文字小文字を区別）
3. 異なる座標で描画を試す

### 問題: 変形が正しく見えない
**解決方法：**
1. リセットを試す：`すべての変形をリセット`
2. 頂点位置が極端すぎないか確認
3. 変形の強さを減らす

### 問題: パフォーマンスが遅い
**解決方法：**
1. より小さい画像を使用
2. 更新頻度を減らす（待ち時間を長くする）
3. より単純な変形を使用

## 📚 次のステップ

基本を理解したら、さらに探求しましょう：

1. **完全なドキュメントを読む**: [README.ja.md](README.ja.md)
2. **高度な例を試す**: [EXAMPLES.ja.md](EXAMPLES.ja.md)
3. **実験する**: 独自の変形を作成！
4. **共有する**: プロジェクトをコミュニティに見せる

## 🎓 学習パス

### 初級レベル
1. プリセット変形を使用
2. 強さの値を調整
3. シンプルなアニメーションを作成

### 中級レベル
1. 個別の頂点を制御
2. 複数の変形を組み合わせ
3. マウス/キーボードでインタラクション

### 上級レベル
1. カスタム変形パターンを作成
2. 複雑なアニメーションを実装
3. 他の拡張機能と組み合わせ

## 💬 ヘルプが必要ですか？

困ったときは：
1. [EXAMPLES.ja.md](EXAMPLES.ja.md)でより多くの例を確認
2. 詳細なドキュメントは[README.ja.md](README.ja.md)を確認
3. バグを見つけたらGitHubで問題を報告

## 🎉 楽しんでください！

Live2D エフェクト拡張機能は多くの創造的な可能性を開きます。恐れずに実験して新しいことを試してください。楽しく作成してください！

---

**クイックリファレンスカード**

| ブロック | 目的 |
|-------|---------|
| `コスチュームから画像を読み込む` | 変形する画像を読み込む |
| `[プリセット] 変形を適用 強さ: (%)` | 素早い変形を適用 |
| `頂点 [頂点] を x: y: に設定` | 個別の角を制御 |
| `変形した画像を x: y: に描画` | 結果を表示 |
| `すべての変形をリセット` | 最初からやり直す |
| `画像の幅` / `画像の高さ` | 画像の寸法を取得 |

**頂点：**
- 左上 (TL) - 左上角
- 右上 (TR) - 右上角
- 左下 (BL) - 左下角
- 右下 (BR) - 右下角

**プリセット：**
- 台形 - 上部を狭める
- 遠近法 - 3D奥行き効果
- 波 - 波打つ歪み
- 膨張 - 外側に拡張
