# Live2D エフェクト拡張機能 - 使用例

このドキュメントは、TurboWarpプロジェクトでLive2D エフェクト拡張機能を使用する詳細な例を提供します。

## 目次

1. [はじめに](#はじめに)
2. [基本例](#基本例)
3. [アニメーション例](#アニメーション例)
4. [高度なテクニック](#高度なテクニック)
5. [創造的プロジェクト](#創造的プロジェクト)

## はじめに

変形エフェクトを使用する前に、まず画像を読み込む必要があります：

### コスチュームから読み込む

```scratch
緑の旗がクリックされたとき
コスチューム [sprite1-a] から画像を読み込む
```

### URLから読み込む

```scratch
緑の旗がクリックされたとき
URL [https://example.com/image.png] から画像を読み込む
```

## 基本例

### 例1: シンプルな台形

画像の上部を狭めて台形を作成します。

```scratch
緑の旗がクリックされたとき
コスチューム [character-1] から画像を読み込む
[台形 ▾] 変形を適用 強さ: (50)%
変形した画像を x: (0) y: (0) に描画
```

**結果**: 画像は上辺が狭くなった台形として表示されます。

### 例2: 遠近ビュー

3D効果を作成するために遠近法変形を適用します。

```scratch
緑の旗がクリックされたとき
コスチューム [background] から画像を読み込む
[遠近法 ▾] 変形を適用 強さ: (70)%
変形した画像を x: (0) y: (0) に描画
```

**結果**: 画像は角度から見られているように、遠くに後退する壁のように表示されます。

### 例3: 波の歪み

画像を波打たせる波効果を適用します。

```scratch
緑の旗がクリックされたとき
コスチューム [flag] から画像を読み込む
[波 ▾] 変形を適用 強さ: (40)%
変形した画像を x: (0) y: (0) に描画
```

**結果**: 画像の左右の端が波のパターンで歪みます。

### 例4: 膨張効果

画像をすべての辺から外側に膨らませます。

```scratch
緑の旗がクリックされたとき
コスチューム [ball] から画像を読み込む
[膨張 ▾] 変形を適用 強さ: (60)%
変形した画像を x: (0) y: (0) に描画
```

**結果**: 画像は風船のような効果で外側に膨らんで表示されます。

## アニメーション例

### 例5: はためく旗のアニメーション

風になびく旗のリアルなアニメーションを作成します。

```scratch
緑の旗がクリックされたとき
コスチューム [flag] から画像を読み込む
[時間 v] を (0) にする
ずっと
  [時間 v] を (5) ずつ変える
  [波1 v] を ((時間 の sin) × (15)) にする
  [波2 v] を (((時間) + (90) の sin) × (15)) にする
  頂点 [右上 ▾] を x: (100) y: (波1) に設定
  頂点 [右下 ▾] を x: (100) y: ((100) + (波2)) に設定
  変形した画像を x: (0) y: (0) に描画
end
```

**結果**: 旗が前後にスムーズにはためきます。

### 例6: 脈打つ効果

リズミカルな膨張効果で画像を脈打たせます。

```scratch
緑の旗がクリックされたとき
コスチューム [heart] から画像を読み込む
ずっと
  (10) 回繰り返す
    [強さ v] を ((i) × (10)) にする
    [膨張 ▾] 変形を適用 強さ: (強さ)%
    変形した画像を x: (0) y: (0) に描画
    (0.05) 秒待つ
  end
  (10) 回繰り返す
    [強さ v] を ((100) - ((i) × (10))) にする
    [膨張 ▾] 変形を適用 強さ: (強さ)%
    変形した画像を x: (0) y: (0) に描画
    (0.05) 秒待つ
  end
end
```

**結果**: ハートが継続的に内外に脈打ちます。

### 例7: 回転する台形

異なる台形の強さを循環します。

```scratch
緑の旗がクリックされたとき
コスチューム [character-1] から画像を読み込む
[方向 v] を (1) にする
[強さ v] を (0) にする
ずっと
  [強さ v] を ((方向) × (2)) ずつ変える
  もし <(強さ) > (100)> なら
    [方向 v] を (-1) にする
  end
  もし <(強さ) < (0)> なら
    [方向 v] を (1) にする
  end
  [台形 ▾] 変形を適用 強さ: (強さ)%
  変形した画像を x: (0) y: (0) に描画
  (0.03) 秒待つ
end
```

**結果**: 台形効果がスムーズに増減します。

### 例8: キャラクターの首の傾き

頂点を操作してキャラクターの首の傾きをシミュレートします。

```scratch
緑の旗がクリックされたとき
コスチューム [character-face] から画像を読み込む
[角度 v] を (0) にする
ずっと
  [角度 v] を (2) ずつ変える
  [傾きX v] を ((角度 の sin) × (10)) にする
  [傾きY v] を ((角度 の cos) × (5)) にする
  頂点 [右上 ▾] を x: ((100) + (傾きX)) y: (傾きY) に設定
  頂点 [左上 ▾] を x: ((0) - (傾きX)) y: (傾きY) に設定
  変形した画像を x: (0) y: (0) に描画
  (0.05) 秒待つ
end
```

**結果**: キャラクターの顔がスムーズに前後に傾きます。

## 高度なテクニック

### 例9: インタラクティブなマウス制御

マウスの位置で変形を制御します。

```scratch
緑の旗がクリックされたとき
コスチューム [sprite] から画像を読み込む
ずっと
  [マウスX正規化 v] を (((マウスのx座標) + (240)) / (480)) にする
  [マウスY正規化 v] を (((マウスのy座標) + (180)) / (360)) にする
  [強さ v] を ((マウスX正規化) × (100)) にする
  [遠近法 ▾] 変形を適用 強さ: (強さ)%
  変形した画像を x: (0) y: (0) に描画
end
```

**結果**: 遠近法効果がマウスのX位置に基づいて変化します。

### 例10: 手動ダイヤモンド形状

4つの頂点すべてを手動で設定してダイヤモンド形状を作成します。

```scratch
緑の旗がクリックされたとき
コスチューム [gem] から画像を読み込む
[サイズ v] を (100) にする
[半分サイズ v] を ((サイズ) / (2)) にする
頂点 [左上 ▾] を x: (半分サイズ) y: (0) に設定
頂点 [右上 ▾] を x: (サイズ) y: (半分サイズ) に設定
頂点 [右下 ▾] を x: (半分サイズ) y: (サイズ) に設定
頂点 [左下 ▾] を x: (0) y: (半分サイズ) に設定
変形した画像を x: (0) y: (0) に描画
```

**結果**: 画像がダイヤモンド形状に変形されます。

### 例11: 画面揺れ効果

頂点をランダムにオフセットして画面揺れを作成します。

```scratch
緑の旗がクリックされたとき
コスチューム [screen] から画像を読み込む
(20) 回繰り返す
  頂点 [左上 ▾] を x: ((-5) から (5) までの乱数) y: ((-5) から (5) までの乱数) に設定
  頂点 [右上 ▾] を x: ((100) + ((-5) から (5) までの乱数)) y: ((-5) から (5) までの乱数) に設定
  頂点 [左下 ▾] を x: ((-5) から (5) までの乱数) y: ((100) + ((-5) から (5) までの乱数)) に設定
  頂点 [右下 ▾] を x: ((100) + ((-5) から (5) までの乱数)) y: ((100) + ((-5) から (5) までの乱数)) に設定
  変形した画像を x: (0) y: (0) に描画
  (0.03) 秒待つ
end
すべての変形をリセット
変形した画像を x: (0) y: (0) に描画
```

**結果**: 画像が一瞬激しく揺れ、その後通常に戻ります。

### 例12: 状態間のスムーズな遷移

ある変形から別の変形へスムーズにアニメーションします。

```scratch
緑の旗がクリックされたとき
コスチューム [sprite] から画像を読み込む

// 初期状態を保存
[開始TL_X v] を (0) にする
[開始TL_Y v] を (0) にする
[開始TR_X v] を (100) にする
[開始TR_Y v] を (0) にする

// ターゲット変形を適用
[遠近法 ▾] 変形を適用 強さ: (75)%

// ターゲット状態を保存
[終了TL_X v] を (頂点 [左上 ▾] のx) にする
[終了TL_Y v] を (頂点 [左上 ▾] のy) にする
[終了TR_X v] を (頂点 [右上 ▾] のx) にする
[終了TR_Y v] を (頂点 [右上 ▾] のy) にする

// リセットしてアニメーション
すべての変形をリセット

(30) 回繰り返す
  [進行度 v] を ((i) / (30)) にする
  
  [現在TL_X v] を ((開始TL_X) + (((終了TL_X) - (開始TL_X)) × (進行度))) にする
  [現在TL_Y v] を ((開始TL_Y) + (((終了TL_Y) - (開始TL_Y)) × (進行度))) にする
  
  頂点 [左上 ▾] を x: (現在TL_X) y: (現在TL_Y) に設定
  頂点 [右上 ▾] を x: ((開始TR_X) + (((終了TR_X) - (開始TR_X)) × (進行度))) y: ((開始TR_Y) + (((終了TR_Y) - (開始TR_Y)) × (進行度))) に設定
  
  変形した画像を x: (0) y: (0) に描画
  (0.03) 秒待つ
end
```

**結果**: 画像が通常から遠近ビューへスムーズに遷移します。

## 創造的プロジェクト

### 例13: 雨水たまりの反射

水たまりのように歪む反射効果を作成します。

```scratch
緑の旗がクリックされたとき
コスチューム [character] から画像を読み込む

// 通常のキャラクターを描画
変形した画像を x: (0) y: (100) に描画

// 反射を描画
頂点 [左上 ▾] を x: (5) y: (0) に設定
頂点 [右上 ▾] を x: (95) y: (0) に設定
頂点 [左下 ▾] を x: (0) y: (100) に設定
頂点 [右下 ▾] を x: (100) y: (100) に設定

// 反射に波を追加
ずっと
  [時間 v] を ((タイマー) × (100)) にする
  [波 v] を ((時間 の sin) × (3)) にする
  頂点 [左上 ▾] を x: ((5) + (波)) y: (0) に設定
  頂点 [右上 ▾] を x: ((95) + (波)) y: (0) に設定
  変形した画像を x: (0) y: (-100) に描画
end
```

**結果**: 水のように波打つ反射。

### 例14: ドアが開く効果

遠近法でドアが開くようにシミュレートします。

```scratch
緑の旗がクリックされたとき
コスチューム [door] から画像を読み込む
[開き具合 v] を (0) にする

(20) 回繰り返す
  [開き具合 v] を (5) ずつ変える
  
  頂点 [右上 ▾] を x: ((100) - (開き具合)) y: ((開き具合) × (0.3)) に設定
  頂点 [右下 ▾] を x: ((100) - (開き具合)) y: ((100) - ((開き具合) × (0.3))) に設定
  
  変形した画像を x: (0) y: (0) に描画
  (0.05) 秒待つ
end
```

**結果**: ドアが遠近法で開くように見えます。

### 例15: カーテン効果

劇場のカーテンが開く効果を作成します。

```scratch
緑の旗がクリックされたとき
コスチューム [curtain] から画像を読み込む

(50) 回繰り返す
  [開き度 v] を ((i) × (2)) にする
  
  // 左のカーテン半分
  頂点 [右上 ▾] を x: ((50) - (開き度)) y: (0) に設定
  頂点 [右下 ▾] を x: ((50) - (開き度)) y: (100) に設定
  変形した画像を x: (-50) y: (0) に描画
  
  // 右のカーテン半分
  すべての変形をリセット
  頂点 [左上 ▾] を x: ((50) + (開き度)) y: (0) に設定
  頂点 [左下 ▾] を x: ((50) + (開き度)) y: (100) に設定
  変形した画像を x: (50) y: (0) に描画
  
  (0.03) 秒待つ
end
```

**結果**: カーテンが中央から開きます。

## ヒントとベストプラクティス

### パフォーマンスのヒント

1. **更新頻度を減らす**: 必要でない場合は毎フレーム更新しない
2. **適切な画像サイズを使用**: 小さい画像は速く変形
3. **変形をキャッシュ**: 再計算を避けるために変数に頂点位置を保存

### 視覚品質のヒント

1. **スムーズなアニメーション**: 滑らかな動きのために小さな増分変更を使用
2. **効果を組み合わせる**: 複雑な効果のために複数の変形画像を重ねる
3. **変更間でリセット**: 新しいプリセットを適用する前に`すべての変形をリセット`を呼び出す

### 創造的なヒント

1. **負の値で実験**: 頂点位置は0-100の範囲外にも設定可能
2. **Scratchエフェクトと組み合わせ**: 変形と一緒に色/透明度エフェクトを使用
3. **複数の画像を重ねる**: キャラクターの異なる部分を別々に変形

## 一般的なパターン

### パターン: 呼吸アニメーション

```scratch
緑の旗がクリックされたとき
コスチューム [character] から画像を読み込む
ずっと
  (20) 回繰り返す
    [サイズ v] を ((i) × (2)) にする
    [膨張 ▾] 変形を適用 強さ: (サイズ)%
    変形した画像を x: (0) y: (0) に描画
    (0.05) 秒待つ
  end
  (20) 回繰り返す
    [サイズ v] を ((40) - ((i) × (2))) にする
    [膨張 ▾] 変形を適用 強さ: (サイズ)%
    変形した画像を x: (0) y: (0) に描画
    (0.05) 秒待つ
  end
end
```

### パターン: 風の突風

```scratch
[スペース v] キーが押されたとき
コスチューム [tree] から画像を読み込む
(10) 回繰り返す
  [曲がり v] を ((i) × (3)) にする
  頂点 [右上 ▾] を x: ((100) + (曲がり)) y: (0) に設定
  頂点 [左上 ▾] を x: (曲がり) y: (0) に設定
  変形した画像を x: (0) y: (0) に描画
  (0.05) 秒待つ
end
(10) 回繰り返す
  [曲がり v] を ((30) - ((i) × (3))) にする
  頂点 [右上 ▾] を x: ((100) + (曲がり)) y: (0) に設定
  頂点 [左上 ▾] を x: (曲がり) y: (0) に設定
  変形した画像を x: (0) y: (0) に描画
  (0.05) 秒待つ
end
すべての変形をリセット
```

### パターン: スクワッシュとストレッチでバウンス

```scratch
緑の旗がクリックされたとき
コスチューム [ball] から画像を読み込む
[y v] を (150) にする
[速度 v] を (0) にする

ずっと
  [速度 v] を (-1) ずつ変える
  [y v] を (速度) ずつ変える
  
  もし <(y) < (0)> なら
    [y v] を (0) にする
    [速度 v] を ((速度) × (-0.8)) にする
    
    // 衝撃時にスクワッシュ
    頂点 [左上 ▾] を x: (-10) y: (20) に設定
    頂点 [右上 ▾] を x: (110) y: (20) に設定
    頂点 [左下 ▾] を x: (-10) y: (100) に設定
    頂点 [右下 ▾] を x: (110) y: (100) に設定
  でなければ
    // 空中でストレッチ
    頂点 [左上 ▾] を x: (10) y: (0) に設定
    頂点 [右上 ▾] を x: (90) y: (0) に設定
    頂点 [左下 ▾] を x: (10) y: (100) に設定
    頂点 [右下 ▾] を x: (90) y: (100) に設定
  end
  
  変形した画像を x: (0) y: (y) に描画
  (0.03) 秒待つ
end
```

## デバッグ例のトラブルシューティング

変形が期待通りに動作しない場合は、これらのデバッグアプローチを試してください：

### デバッグ: 頂点位置を表示

```scratch
緑の旗がクリックされたとき
コスチューム [sprite] から画像を読み込む
[台形 ▾] 変形を適用 強さ: (50)%
((左上: ) と ((頂点 [左上 ▾] のx) と ((,) と (頂点 [左上 ▾] のy) を結合) を結合) を結合) を (2) 秒言う
((右上: ) と ((頂点 [右上 ▾] のx) と ((,) と (頂点 [右上 ▾] のy) を結合) を結合) を結合) を (2) 秒言う
```

### デバッグ: 段階的プリセット適用

```scratch
緑の旗がクリックされたとき
コスチューム [sprite] から画像を読み込む
(10) 回繰り返す
  [強さ v] を ((i) × (10)) にする
  [遠近法 ▾] 変形を適用 強さ: (強さ)%
  変形した画像を x: (0) y: (0) に描画
  ((強さ: ) と (強さ) を結合) を (0.5) 秒言う
end
```

## まとめ

これらの例は、プロジェクトでLive2D エフェクト拡張機能を使用するための強固な基盤を提供します。恐れずに実験して、異なるテクニックを組み合わせてユニークな効果を作成してください！

詳細については、メインの[README.ja.md](README.ja.md)ファイルを参照してください。
